# Unified regression set for the left bend at the other end

Four visual failures were run, the original image path and SHA256 were retained, and the original files were not moved. It can be used for frame-by-frame geometry inspection, but cannot be used as a complete continuous camera sequence: the original script saves a snapshot in about 0.1 seconds, and the CSV records frame by frame, and the two numbers are different. You may not interpolate missing images or claim to have completed full frame timing verification.

Latest run 141033: 72 frames paired, 2 frames width_estimated, 75th frame unavailable; all 75 frames fresh. The failed frame model has 5 paired anchor points, but the height only covers 57.5% to 67.5%, and the span 10% is less than the required 15%, and is rejected before fitting. Not an I/O error or processing timeout. Reducing the condition by only 15% will change the model credibility and cannot be directly deployed accordingly.

Follow-up plan:
1. Use all saved frames to uniformly compare direct pairings and independent boundary fitting, without selecting parameters for individual failure graphs. Distinguish between tracks that are rejected by the algorithm and those that are correctly rejected without tracks; establish near and far center/boundary ranges for manual review.
2. Track the continuous white line and yellow marking line independently, and then estimate the center from multiple spatial points; retain the estimation source and confidence. Don't rely on a fixed sampling height that must have both yellow and white lines.
3. The timing width or curve prior must have timestamp, validity period and visible boundary consistency check, and old steering cannot be blindly reused. Verify line loss and recovery using real continuous images.
4. When the existing data is insufficient, only collect frame-by-frame images of the problem area when the motor is powered off and manually pushed slowly; independent acquisition programs must not import the PWM library. This data does not replace dynamic driving verification.
5. After passing unified offline sets, negative samples and real-time static performance checks, a restricted real vehicle verification is designed. Users are not required to repeatedly test run.
