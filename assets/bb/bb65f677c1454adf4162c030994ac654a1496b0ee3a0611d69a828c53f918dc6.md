# 完整窗口版本另一端左弯失败

运行20260908_134855_688477，第69帧重放selected / insufficient_pairs / unavailable，与日志一致。

本帧白线与黄色在y=102、96、72、66处都有候选；y=90、84、78处没有黄色候选。近组102/96与远组72/66之间隔4个采样步长，超过连续配对允许的3。因此两组各只有2个节点，无法形成至少3节点的连续路径。不是远处黄色窗口仍被截断，也不是总体只有两个候选。

现有输出没有近目标；即使仅放宽跨带连接，y=78近目标仍需要在y=72与96之间插值，跨度0.20超过target函数0.15的上限。因此只改一个间隔阈值不能完整解决，且不能未经验证同时放宽两项。

日志末帧请求停止370/回中365，无I/O错误。当前完整窗口修复不等于消除黄色虚线造成的稀疏配对。下一步离线检查y=78附近原始黄色信息和更密采样的可行性，再确定模型方案；不自动改部署版本。
