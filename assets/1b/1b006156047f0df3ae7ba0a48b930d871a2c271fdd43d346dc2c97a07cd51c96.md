# 另一端左弯统一回归集

四次视觉失败运行，保留原图路径与SHA256，不移动原文件。可用于逐帧几何检查，不能当作完整连续摄像头序列：原脚本约0.1秒保存一次快照，CSV逐帧记录，两者数量不同。不得插值生成缺失图像或宣称已完成全帧时序验证。

最新运行141033：72帧paired、2帧width_estimated、第75帧unavailable；所有75帧fresh。失败帧模型有5个配对锚点，但高度仅覆盖57.5%至67.5%，跨度10%小于所需15%，拟合前拒绝。不是I/O错误或处理超时。仅降低15%条件会改变模型可信度，不能据此直接部署。

后续方案：
1. 用全部保存帧统一比较直接配对、独立边界拟合，不针对单个失败图挑参数。区分有赛道但算法拒绝与无赛道正确拒绝；建立人工审核的近远中心/边界范围。
2. 独立跟踪连续白线与黄色标线，再由多个空间点估计中心；保留估计来源与置信度。不要依赖固定采样高度必须同时有黄白线。
3. 时序宽度或曲线先验必须带时间戳、有效期和可见边界一致性检查，不能盲目复用旧转向。使用真实连续图像验证丢线和恢复。
4. 现有资料不足时，只采集电机断电下手动缓慢推过问题区的逐帧图像；独立采集程序不得导入PWM库。该数据不代替动态驾驶验证。
5. 通过统一离线集、负样本与实时静态性能检查后，才设计一次受限实车验证。暂不要求用户反复试跑。
