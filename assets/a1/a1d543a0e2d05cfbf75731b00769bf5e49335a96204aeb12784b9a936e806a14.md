# 第222帧二次拟合对照

候选只在局部直线版本拒绝后尝试二次曲线；至少5点、至少10%高度覆盖、最多2.5%高度外推，保留1.5像素残差及不确定度界限。不改冻结版本。

第222帧：直线最大残差1.60像素；二次曲线0.85像素。估计近目标71.53，PWM385，前后行驶图重放PWM390与388。

2391张保存图片回归：2313有效保持有效，77不可用保持不可用，仅该帧恢复。此帧参与开发，所以三段全有效不构成独立验证。

留一法检查：逐个删去一个采样点重新拟合，PWM为377、386、385、384、384、386、386、385、388；范围11，留出点最大预测误差2.50像素。说明训练残差变小不能证明曲线可靠，尤其端点对方向影响较大。当前不推荐仅为消除一帧拒绝而部署二次回退。

结论：保留局部直线候选的固定独立结果319/320；二次候选仅作实验，不上传实车。后续评估须关注方向估计敏感性和实测偏差，而非将输出覆盖优化为100%。
