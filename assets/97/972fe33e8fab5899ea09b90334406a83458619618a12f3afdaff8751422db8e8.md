# 近处定位与方向分离实验

两段现已为开发序列；冻结版本未改。

1. 在原55%、60%远目标之外加入57.5%：单帧覆盖不变。
2. 加入62.5%：第二段单帧269→282，第一段312不变。但近远只隔3像素，原方向公式对像素噪声敏感，因此不部署此候选。
3. 局部方向拟合：仅在已有至少4个可靠中心点、跨度≥7.5%且65%近目标可用时拟合斜率；最大残差≤1.5像素，包含0.5像素噪声下限的斜率不确定度对应PWM≤8。输出标记direction_estimated，不伪造远目标。阈值为实验条件，并非校准保证。

方向拟合单帧第二段269→285，第一段312保持。与原0.15秒时序辅助结合后，第一段320/320，第二段311/319（原冻结版本308/319）。剩余失败128–130、170–172、186、310。仍未稳定通过，不生成驾驶脚本。

下一重点仍是当前帧能否形成可靠中心证据；单纯补一个更近的远目标、增加模型复杂度或延长历史有效期都不是已证实的解决方案。尚未完成完整负样本回归与树莓派时延验证。
